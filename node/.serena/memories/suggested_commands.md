# Suggested Commands

## Workspace Root

- `pnpm format` - Prettier formatting
- `pnpm lint` - ESLint
- `pnpm lintfix` - ESLint with --fix
- `pnpm build` - turbo build all packages
- `pnpm dev` - turbo dev --parallel

## Per-package (from package directory)

- `pnpm lint` - ESLint for that package
